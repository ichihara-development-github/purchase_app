<% if current_user %>

<div class = "side-bar right margin-vertical">
    <ul class="list-group">
                <li class="list-group-item center">
                  <%= link_to "買い物かご　", baskets_path %>　　
                  <span class="badge badge-primary badge-pill"><%= current_user.baskets.count %></span><br /><br />
                  小計:　<span class = "price">¥<%= current_user.considering_productions.sum(:price)%></span>
                </li>
              </ul>
 </div>
 
<% end %>