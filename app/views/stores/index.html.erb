<div class = "margin-vertical center">
  <%= link_to "オーナ登録はこちら", registration_path ,class: "btn btn-warning btn-lg btn-block" if current_user && !current_user.seller%>
</div>

<div class="margin-vertical">
  <%= link_to  map_index_path, style:"text-decoration: none"  do%>
  <span style = "
    padding: 15px 40px;
    border-radius: 30px;
    background-color: orange;
    font-size:20px;
    color: black"><i class="fas fa-map-marked-alt"></i>　マップから探す</span>
  <% end %>
</div>


 <div class="row">
    <% @stores.each do |store| %>
  <div class="col-md-6 margin-vertical">
    <div class="card">
        <% if store.top_image.present? %>
            <%= image_tag store.top_image ,class: "img-fluid", style: "height:350px"%>
        <% else %>
            <img class="long-img" src="https://purchase-app-backet.s3-ap-northeast-1.amazonaws.com/uploads/no-image.jpg" alt="Something">
        <% end %>
      <div class="card-body">
            <h5 class="card-title"><%= store.name %></h5>
            <p class="card-text"><%= store.description %></p>
        <%= link_to "Go Shop", store_path(store), class: "btn btn-primary" %>
    </div>
  </div>
  </div>
    <% end %>
     </div>
